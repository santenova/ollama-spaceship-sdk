import type { Config } from '@jest/types';

// Jest 预设配置：定义转译器、模块等基础设置
const jestPreset: Config.InitialOptions = {
  // 核心：指定 jest-ts 的转译器处理 TS/TSX 文件
  transform: {
    '^.+\\.(ts|tsx)$': 'jest-ts/dist/transformer.js'
  },
  // 模块文件扩展名（优先识别 TS/TSX）
  moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json', 'node'],
  // 关闭默认的 coverage 工具（避免冲突）
  coverageProvider: 'v8',
  // 忽略 node_modules 转译（除非有特殊需求）
  transformIgnorePatterns: ['/node_modules/']
};

export default jestPreset;
