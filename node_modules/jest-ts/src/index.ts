/**
 * jest-ts 主入口文件
 * 统一导出转译器、核心函数和类型定义
 */

// 从transformer.ts导入成员（名称必须与导出完全一致）
import transformer, { transformTsCode } from './transformer';
// 导入类型定义
import type { JestTsOptions, CacheData } from './types';

/**
 * 导出转译器（供Jest配置使用）
 */
export {
  transformer,       // Jest转译器实例
  transformTsCode    // 核心转译函数（与transformer.ts导出名称严格匹配）
};

/**
 * 默认导出转译器（兼容默认导入语法）
 */
export default transformer;

/**
 * 导出类型定义（供TypeScript项目使用）
 */
export type {
  JestTsOptions,     // 转译配置选项类型
  CacheData          // 缓存数据结构类型
};

/**
 * 导出版本信息（从package.json读取）
 */
export const version = require('../package.json').version;
