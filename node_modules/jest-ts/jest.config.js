/** @type {import('jest').Config} */
module.exports = {
  preset: "./dist/jest-preset.js",
  testEnvironment: "node",
  moduleFileExtensions: ["ts", "tsx", "js"],
  // 显式配置转译规则，确保测试文件被处理
  transform: {
    "^.+\\.(ts|tsx)$": "./dist/transformer.js"
  },
  // 告诉Jest哪些目录下的文件需要被转译（包括__tests__）
  transformIgnorePatterns: []
};
